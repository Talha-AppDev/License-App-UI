<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.S1"
        tools:targetApi="31">
<!--        <activity-->
<!--            android:name=".act_learner_permit"-->
<!--            android:exported="false" />-->
        <!-- <activity -->
        <!-- android:name=".act_violation_history" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".act_lic_record" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".act_reg_vehicle" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".act_feedback" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".act_traffic_rules" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".act_license_reg" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".challan_payment" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".act_learner_permit"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>